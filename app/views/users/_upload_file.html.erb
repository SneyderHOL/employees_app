<div>
  <p>Import employees from a file:</p>
  <%= form_with url: import_users_path, multipart: true do |form| %>
    <%= form.file_field :import_file, accept: "text/csv" %>
    <div class="actions">
      <%= form.submit 'Import' %>
    </div>
  <% end %>
</div>